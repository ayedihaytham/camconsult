# Design system — moved

The concise, current CamConsult design guide now lives at
[`../docs/design-system.md`](../docs/design-system.md).

Use that document as the canonical source for new work. Runtime tokens remain in
`src/index.css` and `tailwind.config.js`. This compatibility file is retained so
older links do not break; the former migration-era specification was removed
because several implementation-status claims had become stale.
